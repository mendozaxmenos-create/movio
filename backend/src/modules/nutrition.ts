/**
 * Espacio reservado para nutrición (macros, calorías, planes) además del peso.
 * Reutilizar el mismo patrón de repositorio y endpoints REST por recurso.
 */
export type NutritionModuleScope = 'future';
