/**
 * Espacio reservado para el módulo de hábitos (tracking, streaks, recordatorios).
 * La lógica de dominio debería vivir en `domain/` y exponer casos de uso en `application/`.
 */
export type HabitsModuleScope = 'future';
